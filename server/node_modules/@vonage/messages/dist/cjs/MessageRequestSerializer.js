"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// lib/MessageRequestSerializer.ts
var MessageRequestSerializer_exports = {};
__export(MessageRequestSerializer_exports, {
  messageRequestSerializer: () => messageRequestSerializer
});
module.exports = __toCommonJS(MessageRequestSerializer_exports);
var import_server_client = require("@vonage/server-client");
var messageRequestSerializer = (params) => {
  const data = import_server_client.Client.transformers.snakeCaseObjectKeys(params, true);
  const channel = params?.channel;
  switch (channel) {
    case "rcs" /* RCS */:
      if ("custom" in data) {
        data.custom = params.custom;
      }
      break;
    case "email" /* EMAIL */:
      if ("email" in data && Array.isArray(data.email.cc) && data.email.cc.length < 1) {
        data.email.cc = void 0;
      }
      if ("email" in data && Array.isArray(data.email.bcc) && data.email.bcc.length < 1) {
        data.email.bcc = void 0;
      }
      break;
    default:
      break;
  }
  return data;
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  messageRequestSerializer
});
