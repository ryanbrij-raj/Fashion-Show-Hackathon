import { Channels } from '../../enums/Channels.js';
import { AbstractMessage } from '../AbstractMessage.js';
import { EmailParams } from '../../types/Channels/Email/EmailParams.js';
import { EmailSettings } from '../../types/Channels/Email/EmailSettings.js';
import '../../enums/MessageTypes.js';
import '../../types/MessageParams.js';
import '../../enums/Email/EmailImportance.js';
import '../../enums/Email/EmailPriority.js';
import '../../enums/Email/EmailSensitivity.js';
import '../../types/Channels/Email/EmailRecipient.js';

/**
 * Shared class for all Email Messages
 *
 * @group Email
 */
declare abstract class AbstractEmailMessage extends AbstractMessage implements EmailParams {
    /**
     * The channel for this message (always 'email').
     */
    channel: Channels.EMAIL;
    /**
     * An object of settings for the email message.
     */
    email: EmailSettings;
    /**
     */
    constructor(params: Omit<EmailParams, 'channel' | 'messageType'>);
}

export { AbstractEmailMessage };
