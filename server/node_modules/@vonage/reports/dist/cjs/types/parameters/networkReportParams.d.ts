/**
 * Mobile network code filter parameter for report requests.
 */
type NetworkReportParams = {
    /**
     * Mobile network code (MCC/MNC) to filter records by.
     */
    network?: string;
};

export type { NetworkReportParams };
