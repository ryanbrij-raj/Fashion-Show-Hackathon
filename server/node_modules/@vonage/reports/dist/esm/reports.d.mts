import './enums/asyncReportStatus.mjs';
import './enums/direction.mjs';
import './enums/messageStatus.mjs';
import './enums/product.mjs';
import './enums/messageProvider.mjs';
import './enums/smsStatus.mjs';
import './enums/verifyChannel.mjs';
import './enums/voiceStatus.mjs';
import { AnyReportParams, AnyAsyncReportParams } from './types/parameters/index.mjs';
import './types/records/index.mjs';
import { AsyncReportResponse } from './types/responses/asyncReportResponse.mjs';
import { SyncRecordsResponse } from './types/responses/syncRecordsResponse.mjs';
import { FileClient, AuthenticationType } from '@vonage/server-client';
import './types/parameters/asyncReportParams.mjs';
import './types/parameters/baseReportParams.mjs';
import './types/records/conversationEventRecord.mjs';
import './types/parameters/conversationEventReportParams.mjs';
import './types/records/conversationMessagesRecord.mjs';
import './types/parameters/conversationReportParams.mjs';
import './types/parameters/countryReportParams.mjs';
import './types/parameters/dateReportParams.mjs';
import './types/parameters/directionReportParams.mjs';
import './types/records/inAppVoiceRecord.mjs';
import './types/parameters/inAppVoiceReportParams.mjs';
import './types/records/messageInboundRecord.mjs';
import './types/records/messageOutboundRecord.mjs';
import './types/parameters/messageReportParams.mjs';
import './types/records/networkApiEventRecord.mjs';
import './types/parameters/networkAPIEventReportParams.mjs';
import './types/records/networkRecord.mjs';
import './types/parameters/networkReportParams.mjs';
import './types/records/numberInsightRecord.mjs';
import './types/parameters/numberInsightV1ReportParams.mjs';
import './types/parameters/recipientReportParams.mjs';
import './types/records/recordCommon.mjs';
import './types/records/reportUsageRecord.mjs';
import './types/parameters/reportUsageReportParams.mjs';
import './types/records/smsInboundRecord.mjs';
import './types/records/smsOutboundRecord.mjs';
import './types/parameters/smsReportParams.mjs';
import './types/records/smsTrafficControlRecord.mjs';
import './types/parameters/smsTrafficControlReportParams.mjs';
import './types/parameters/senderReportParams.mjs';
import './types/records/verifyAPIRecord.mjs';
import './types/parameters/verifyReportParams.mjs';
import './types/records/verifyV2Record.mjs';
import './types/parameters/verifyV2ReportParams.mjs';
import './types/records/videoAPIRecord.mjs';
import './types/parameters/videoAPIReportParams.mjs';
import './types/records/voiceAMDRecord.mjs';
import './types/parameters/voiceAMDReportParams.mjs';
import './types/parameters/voiceAPIReportParams.mjs';
import './types/records/voiceASRRecord.mjs';
import './types/parameters/voiceASRReportParams.mjs';
import './types/records/voiceCallRecord.mjs';
import './types/records/voiceFailedRecord.mjs';
import './types/parameters/voiceFailedReportParams.mjs';
import './types/records/voiceTTSRecord.mjs';
import './types/parameters/voiceTTSReportParams.mjs';
import './types/records/voiceWebSocketRecord.mjs';
import './types/parameters/voiceWebsocketReportParams.mjs';

/**
 * Client class to interact with the Vonage Reports API, which enables
 * users to request reports of activity for their Vonage account.
 *
 * @remarks
 * Supports both synchronous reports (immediate, smaller datasets) and
 * asynchronous reports (background processing for large datasets up to
 * tens of millions of records).
 *
 * @example
 * Create a standalone Reports client
 *
 * ```ts
 * import { Reports } from '@vonage/reports';
 *
 * const reportsClient = new Reports({
 *   apiKey: VONAGE_API_KEY,
 *   apiSecret: VONAGE_API_SECRET,
 * });
 * ```
 */
declare class Reports extends FileClient {
    protected authType: AuthenticationType;
    /**
     * Retrieves records synchronously based on the provided parameters.
     * Optimized for frequent, periodic retrievals of small batches of
     * data (up to tens of thousands of records per query).
     *
     * @param {AnyReportParams} params - Parameters for filtering the
     *   records, including required `product` and `accountId`.
     * @return {Promise<SyncRecordsResponse>} A promise that resolves to
     *   a {@link SyncRecordsResponse} containing the matching records and
     *   pagination metadata.
     *
     * @example
     * Get a page of outbound SMS records
     *
     * ```ts
     * const response = await reportsClient.getRecords({
     *   product: Product.SMS,
     *   accountId: VONAGE_API_KEY,
     *   direction: Direction.OUTBOUND,
     *   dateStart: '2024-01-01T00:00:00Z',
     *   dateEnd: '2024-01-02T00:00:00Z',
     * });
     *
     * console.log(`Total records: ${response.itemsCount}`);
     * for (const record of response.records) {
     *   console.log(record.id);
     * }
     * ```
     */
    getRecords(params: AnyReportParams): Promise<SyncRecordsResponse>;
    /**
     * Creates an asynchronous report generation request. The report is
     * processed in the background and can be polled with
     * {@link getReport}. Optimized for infrequent, large data queries.
     *
     * @remarks
     * Vonage recommends limiting async queries to a maximum of 7 million
     * records by setting `dateStart` and `dateEnd` accordingly. On
     * average, the API takes 5–10 minutes to generate 1 million records.
     *
     * @param {AnyAsyncReportParams} params - Parameters for the report,
     *   including required `product` and `accountId`.
     * @return {Promise<AsyncReportResponse>} A promise that resolves to
     *   an {@link AsyncReportResponse} containing the report ID and
     *   initial status.
     *
     * @example
     * Create an async SMS report
     *
     * ```ts
     * const report = await reportsClient.createReport({
     *   product: Product.SMS,
     *   accountId: VONAGE_API_KEY,
     *   dateStart: '2024-01-01T00:00:00Z',
     *   dateEnd: '2024-02-01T00:00:00Z',
     *   callbackUrl: 'https://example.com/webhook',
     * });
     *
     * console.log(`Report ID: ${report.requestId}`);
     * console.log(`Status: ${report.requestStatus}`);
     * ```
     */
    createReport(params: AnyAsyncReportParams): Promise<AsyncReportResponse>;
    /**
     * Retrieves the status and details of an asynchronous report by its
     * report ID.
     *
     * @remarks
     * Reports are retained for 4 days. Requests for reports older than
     * 4 days will return a 404.
     *
     * @param {string} reportId - The unique identifier of the report
     *   (`requestId` from the {@link createReport} response).
     * @return {Promise<AsyncReportResponse>} A promise that resolves to
     *   an {@link AsyncReportResponse} with the current report status
     *   and download link when complete.
     *
     * @example
     * Poll a report until it is complete
     *
     * ```ts
     * let report = await reportsClient.getReport(reportId);
     *
     * while (report.requestStatus === AsyncReportStatus.PENDING
     *   || report.requestStatus === AsyncReportStatus.PROCESSING) {
     *   await new Promise((resolve) => setTimeout(resolve, 5000));
     *   report = await reportsClient.getReport(reportId);
     * }
     *
     * console.log(`Download: ${report.links.downloadReport?.href}`);
     * ```
     */
    getReport(reportId: string): Promise<AsyncReportResponse>;
    /**
     * Cancels a pending or processing asynchronous report. Reports that
     * have already completed cannot be cancelled.
     *
     * @param {string} reportId - The unique identifier of the report to
     *   cancel (`requestId` from the {@link createReport} response).
     * @return {Promise<AsyncReportResponse>} A promise that resolves to
     *   an {@link AsyncReportResponse} reflecting the cancelled status.
     *
     * @example
     * Cancel a report
     *
     * ```ts
     * const report = await reportsClient.cancelReport(reportId);
     * console.log(`Status: ${report.requestStatus}`);
     * ```
     */
    cancelReport(reportId: string): Promise<AsyncReportResponse>;
    /**
     * Download the report of a call to the specified file path.
     *
     * Calls `getReport` to fecth the url to downlaod the file report. If the
     * report does not have `_links.download_report` set, then this will throw
     * an error.
     *
     * @param {string} reportId - The UUID of the report to download
     * @param {string} path - The local file path where the report will be saved.
     * @return {Promise<void>} A promise that resolves when the report has been successfully downloaded.
     *
     * @example
     * ```ts
     * await voiceClient.downloadReport(REPORT_UUID, './report.csv');
     * ```
     */
    getReportData(reportId: string, path: string): Promise<void>;
}

export { Reports };
