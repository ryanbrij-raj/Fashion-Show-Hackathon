/**
 * Sender filter parameter for report requests.
 */
type SenderReportParams = {
    /**
     * Sender ID or phone number to filter records by.
     */
    from?: string;
};

export type { SenderReportParams };
